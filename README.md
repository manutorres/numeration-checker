Numeration Checker

Python script that scans a list of numbered files in a directory and applies corrective
renaming if necessary so that file managers can correctly sort them.

Useful for keeping lessons and courses folders in meaningful order.